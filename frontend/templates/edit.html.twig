<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html; charset=utf-8" />
</head>
<body>
  <h1>記事編集</h1>

  <a href="{{constant('koujigenba_php\\backend\\Bootstrap::ENTRY_URL')}}">トップページ</a>

  <form method="post" action="list.php" enctype="multipart/form-data">
    <div>
      <label for="title">タイトル</label>
      <input type="title" id="title" name="title" value="{{article.saveArr.title}}">
    </div>
    {% if articleErr.submitArr.title | length > 0 %}{{articleErr.submitArr.title}}{% endif %}
    <div>
      <label for="content">本文</label>
      <textarea id="content" name="content" value="">{{article.saveArr.content}}</textarea>
    </div>
    {% if articleErr.submitArr.content | length > 0 %}{{articleErr.submitArr.content}}{% endif %}
    <div>
      <input type="file" name="image">
    </div>
    {% if articleErr.imageArr.size | length > 0 %}{{articleErr.imageArr.size}}{% endif %}
    {% if articleErr.imageArr.extension | length > 0 %}{{articleErr.imageArr.extension}}{% endif %}
    {% if articleErr.imageArr.name | length > 0 %}{{articleErr.imageArr.name}}{% endif %}
    <div>
      <button type="submit" name="edit" value="edit">編集</button>
    </div>
  </form>
</body>
</html>
