<nav class="navbar navbar-expand-lg navbar-dark bg-success">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{constant('koujigenba_php\\backend\\Bootstrap::ENTRY_URL')}}list.php">工事現場情報サイト</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">

        {% if session.res == true %}
        {# セッションがある場合（ログイン時） #}

          {# 新規投稿 #}
          <li class="nav-item">
            <a class="nav-link" href="{{constant('koujigenba_php\\backend\\Bootstrap::ENTRY_URL')}}create.php">投稿する</a>
          </li>

          {# マイページ #}
          <li class="nav-item">
            <a class="nav-link" href="{{constant('koujigenba_php\\backend\\Bootstrap::ENTRY_URL')}}mypage.php">マイページ</a>
          </li>

          {# ログアウト #}
          <li class="nav-item">
            <a href="#" class="nav-link" onclick="event.preventDefault(); document.getElementById('logout-form').submit();">ログアウト</a>
            <form id="logout-form" method="post" action="list.php">
                <input type="hidden" name="logout" value="logout">
            </form>
          </li>

        {% else %}
          {# セッションがない場合（未ログイン時） #}

          {# ユーザー登録 #}
          <li class="nav-item">
            <a class="nav-link" href="{{constant('koujigenba_php\\backend\\Bootstrap::ENTRY_URL')}}regist.php">ユーザー登録</a>
          </li>

          {# ログイン #}
          <li class="nav-item">
            <a class="nav-link" href="{{constant('koujigenba_php\\backend\\Bootstrap::ENTRY_URL')}}login.php">ログイン</a>
          </li>

        {% endif %}

      </ul>
    </div>
  </div>
</nav>
